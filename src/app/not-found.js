import NotFoundPage from '@/views/NotFoundPage';

export const metadata = {
  title: 'Page Not Found | Mobile Tyre Champions',
  description: 'The page you are looking for does not exist.',
};

export default function NotFound() {
  return <NotFoundPage />;
}
